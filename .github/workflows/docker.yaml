name: Deploy App

on:
    push: 
        branches: [main]
        paths: 
            - app/**
            - docker/**

jobs:
    # run_test:
    #     runs-on: ubuntu-latest
    #     steps:
    #         - uses: actions/checkout@v4
            
    #         - name: set up python
    #           uses: actions/setup-python@v5
    #           with:
    #               python-version: "3.12.x"
            
    #         - name: Install dependencies
    #           run: |
    #             python -m pip install --upgrade pip
    #             pip install --no-cache-dir -r ./app/requirements.txt
    #             pip install pytest
            
    #         - name: Test with pytest
    #           run: pytest tests/test_main.py --doctest-modules --junitxml=junit/test-results-3.12.5.xml

    #         - name: Upload pytest results 
    #           uses: actions/upload-artifact@v4
    #           with:
    #             name: pytest-results-3.12.5
    #             path: junit/test-results-3.12.5.xml
    #           if: ${{always()}}
    
    deploy:
        # needs: run_test
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4

          - name: Login to dockerhub
            uses: docker/login-action@v4
            with:
                username: ${{secrets.DOCKER_USERNAME}}
                password: ${{secrets.DOCKER_PASSWORD}}

          - name: Build image and push to docker hub
            uses: docker/build-push-action@v7
            with:
              # context: .
              file: ./docker/Dockerfile
              push: true
              no-cache: true
              tags: |
                phyuhan/fastapi-app:${{github.sha}}
                phyuhan/fastapi-app:latest
